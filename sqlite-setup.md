# For SQLite setup (alternative)
# You would need to:
# 1. npm install better-sqlite3
# 2. Change drizzle config to use sqlite
# 3. Update db.ts imports

# SQLite DATABASE_URL format:
# DATABASE_URL="file:./database.db"